server:
  port: 8091

spring:
  application:
    name: im-ai-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${IM_AI_DB_URL:jdbc:postgresql://localhost:5432/im_ai}
    username: ${IM_AI_DB_USERNAME:postgres}
    password: ${IM_AI_DB_PASSWORD:123456}
  sql:
    init:
      mode: always
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:sk-d9c53b797786d01b9d7d581cc886cc881c8429609b8ffa51f724d43f84a1b8b9}
      base-url: ${OPENAI_BASE_URL:https://api.team-invite.cn}
      chat:
        options:
          model: ${OPENAI_CHAT_MODEL:gpt5.4}
      embedding:
        options:
          model: ${OPENAI_EMBEDDING_MODEL:text-embedding-3-small}

im:
  ai:
    p2p-queue: im.ai.store-p2p.queue
    group-queue: im.ai.store-group.queue
    default-top-k: 5
    max-summary-messages: 200
    search-candidate-limit: 1000
